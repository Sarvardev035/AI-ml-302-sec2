# AI-ml-302-sec2

This repository now includes a basic machine-learning assignment implementation with:

- **Linear regression** calculation and graph plotting
- **XOR gate model** training with explicit **bias** updates and **sigmoid activation function**

## Run

```bash
python ml_basics.py
```

This generates `linear_regression_plot.svg` and prints XOR predictions.

## Test

```bash
python -m unittest discover -s tests
```
